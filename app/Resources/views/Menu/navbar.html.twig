{% block navbar %}
<nav class="navbar navbar-static-top" role="navigation">
    <!-- Sidebar toggle button-->
    <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
    </a>
    <!-- Navbar Right Menu -->
    <div class="navbar-custom-menu">
        <ul class="nav navbar-nav">
            {% block nabarLinks %}
                {#{% include ':menu:navbarLinks.html.twig' %}#}
            {% endblock %}
            <!-- User Account Menu -->
            <li class="{% if 'profile' in routeName %}active{% endif %} dropdown user user-menu">
                <!-- Menu Toggle Button -->
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <!-- The user image in the navbar-->
                    <img src="{{ asset('bundles/app/img/logo-small.png') }}" height="50" width="50" class="user-image" alt="User Image">
                    <!-- hidden-xs hides the username on small devices so only the image appears. -->
                    <span class="hidden-xs">{{ app.user.email }}</span>
                </a>
                <ul class="dropdown-menu">
                    <!-- The user image in the menu -->
                    <li class="user-header" style="color: black;">
                        <img src="{{ asset('bundles/app/img/logo-small.png') }}" height="50" width="50"  class="img-circle" alt="User Image">
                        <p>
                            {{ app.user.email }}
                            <small>{{ 'pomp.last_login'|trans }} {{ app.user.lastLogin|date }}</small>
                        </p>
                    </li>
                    <!-- Menu Body -->
                    {#<li class="user-body">#}
                        {#<div class="row">#}
                            {#<div class="col-xs-4 text-center">#}
                                {#<a href="#">Followers</a>#}
                            {#</div>#}
                            {#<div class="col-xs-4 text-center">#}
                                {#<a href="#">Sales</a>#}
                            {#</div>#}
                            {#<div class="col-xs-4 text-center">#}
                                {#<a href="#">Friends</a>#}
                            {#</div>#}
                        {#</div>#}
                        <!-- /.row -->
                    {#</li>#}
                    <!-- Menu Footer-->
                    <li class="user-footer">
                        <div class="pull-left">
                            <a href="{{ path('fos_user_profile_show') }}" class="btn btn-default btn-flat">{{ 'button.profile'|trans }}</a>
                        </div>
                        <div class="pull-right">
                            <a href="{{ path('fos_user_security_logout') }}" class="btn btn-default btn-flat">{{ 'button.sign_out'|trans }}</a>
                        </div>
                    </li>
                </ul>
            </li>
            <!-- Control Sidebar Toggle Button -->
            {#<li>#}
                {#<a href="#" data-toggle="control-sidebar"><i class="fa fa-gears"></i></a>#}
            {#</li>#}
        </ul>
    </div>
</nav>
{% endblock %}